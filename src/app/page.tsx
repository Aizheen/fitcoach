export default function Home() {
  // This page is intentionally empty
  // The middleware will redirect unauthenticated users to /login
  // And the (dashboard) route group at / will render for authenticated users
  return null
}
